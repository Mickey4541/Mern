var h1 = React.createElement('h1',null,"Hello from React");

var parent = document.querySelector("#parent")

var root = ReactDOM.createRoot(parent)
root.render(h1)


// This method using cdn is good but we heve to make a complete structure to work with react in a professional and efficient way.So, lets go:: inside react folder
